# vim: filetype=sh

. "${HOME}"/.cargo/env

[ -s "/home/anku/.bun/_bun" ] && source "/home/anku/.bun/_bun"
export BUN_INSTALL="${HOME}/.bun"
export PATH="${BUN_INSTALL}/bin:${PATH}"

export PATH="${HOME}/.local/bin:${PATH}"

export PATH="${HOME}/go/bin:${PATH}"
